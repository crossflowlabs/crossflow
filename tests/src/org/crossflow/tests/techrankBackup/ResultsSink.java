package org.crossflow.tests.techrankBackup;


public class ResultsSink extends ResultsSinkBase {
	
	@Override
	public void consumeRepositorySearchResults(RepositorySearchResult repositorySearchResult) throws Exception {
		
		// TODO: Add implementation that instantiates, sets, and submits result objects (example below)
		//System.out.println("[" + workflow.getName() + "] Result is " + repositorySearchResult.toString() + " (cached=" + repositorySearchResult.isCached() + ")");
	
	}


}
